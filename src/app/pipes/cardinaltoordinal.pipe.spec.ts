import { CardinaltoordinalPipe } from './cardinaltoordinal.pipe';

describe('CardinaltoordinalPipe', () => {
  it('create an instance', () => {
    const pipe = new CardinaltoordinalPipe();
    expect(pipe).toBeTruthy();
  });
});
